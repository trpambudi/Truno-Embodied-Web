---
const { index, eyebrow, title, text = "" } = Astro.props;
---

<div class="section-heading">
  <div class="section-index">{index}</div>
  <div>
    <p class="eyebrow">{eyebrow}</p>
    <h2>{title}</h2>
    {text && <p class="section-text">{text}</p>}
  </div>
</div>